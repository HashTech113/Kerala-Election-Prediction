{
  "name": "kerala-election-prediction",
  "private": true,
  "version": "1.0.0",
  "description": "Root orchestrator for the Kerala Election 2026 FastAPI backend + Vite React frontend.",
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,green \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:backend": "python -m uvicorn main:app --app-dir backend --host 0.0.0.0 --port 8001 --reload",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "install:all": "npm install && npm --prefix frontend install && pip install -r requirements.txt",
    "start": "python -m uvicorn main:app --app-dir backend --host 0.0.0.0 --port 8001"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
